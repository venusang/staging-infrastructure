# Staging Infrastructure

This repository contains Terraform configuration for the staging environment.

## Setup

This repository is connected to Terraform Cloud workspace: `example-staging-workspace`

Changes pushed to the `main` branch will trigger runs in TFC.

## Structure

- `main.tf` - Main infrastructure resources
- `README.md` - This file

## Usage

This workspace is managed by TFC. Push changes to trigger runs automatically.
